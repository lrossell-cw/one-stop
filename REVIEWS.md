# Review Log

Full history of project-level review checkpoints. Append-only — newest entry on top. Never edit or remove a past entry; if something was wrong, add a new entry that corrects it.

See root `OUTLINE.md` for what triggers a review and what happens during one.

---

*(no reviews yet — first entry lands at the first review trigger)*
