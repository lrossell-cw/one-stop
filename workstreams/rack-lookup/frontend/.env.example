# Copy to .env for local dev, or .env.production for a production build.
# URL of the backend (see /backend) — must be reachable from wherever
# this frontend is loaded (a browser on your laptop, your phone, or a
# deployed environment), not just from the machine running Vite.

VITE_API_BASE_URL=http://localhost:3001
